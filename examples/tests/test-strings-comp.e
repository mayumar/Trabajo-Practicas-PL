str1 := 'abCd';
str2 := 'abcd';

si (str1 >= str2) entonces
	escribir('\'' || str1 || '\'' || ' es mayor o igual que ' || '\'' || str2 || '\'');
si_no
	escribir('\'' || str1 || '\'' || ' no es mayor o igual que ' || '\'' || str2 || '\'');
fin_si;

escribir('\n');

